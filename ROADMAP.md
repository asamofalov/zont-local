# План развития ZONT Local

Roadmap фиксирует реализованные возможности и предполагаемое дальнейшее
развитие интеграции. Версии задают порядок работ, но не фиксируют календарные
сроки.

Обозначения:

- **реализовано** — возможность входит в текущую версию;
- **следующий этап** — основной приоритет разработки;
- **запланировано** — следующий функциональный этап;
- **требует подтверждения протокола** — реализация начнётся только после
  проверки поведения на реальном контроллере и фиксации результата в `docs`.

## v0.2.1 — транспорт WebSocket

Статус: **реализовано**.

- настройка через интерфейс Home Assistant;
- одно авторизованное WebSocket-соединение;
- восстановление соединения после обрыва;
- бинарный сенсор подключения и диагностика;
- повторная авторизация и перенастройка адреса и учётных данных контроллера;
- универсальные действия `zont_local.send_command` и `zont_local.send_bulk`;
- публикация незапрошенных сообщений как событий `zont_local_event`.

## v0.2.2 — протокольные запросы

Статус: **реализовано**.

- строгая маршрутизация ответов команд по `id` и `cmdres`;
- внутренние методы для `req_ids`, `req_state` и `scmd`;
- независимая обработка ответов команд, состояний и push-сообщений;
- последовательное выполнение запросов без идентификатора транзакции;
- защита от поздних ответов после таймаута посредством переподключения;
- исключение чувствительных незапрошенных `scmdres` из событий и журналов.

## v0.3 — данные контроллера и представление в Home Assistant

Статус: **реализовано**.

- при добавлении запрашивать серийный номер через `#S54?` и сведения о модели
  через `#S7?`, используя серийный номер как стабильный идентификатор;
- принимать в config flow только локальный IPv4- или IPv6-адрес и формировать
  адрес WebSocket-интерфейса контроллера внутри интеграции;
- ввести типизированную модель данных контроллера и разбирать только поля,
  назначение которых подтверждено документацией или реальным устройством;
- передавать подтверждённые модель контроллера, модель платы, версию прошивки и
  серийный номер в реестр устройств Home Assistant;
- считать ответы `#S54?` и `#S7?` обязательными при добавлении, а последующие
  обновления выполнять в фоне после запуска и восстановления соединения;
- привести названия в интерфейсе к единой схеме:
  - отображаемое имя интеграции — `ZONT Local`, без упоминания конкретного
    WebSocket-транспорта;
  - заголовок config entry — `ZONT <модель> (<адрес>)`;
  - имя устройства — `ZONT <модель>`;
  - поле модели берётся из ответа контроллера вместо заглушки
    `WebSocket Controller`;
- не перезаписывать имена, которые пользователь изменил средствами Home
  Assistant;
- показывать серийный номер только в стандартной карточке устройства и не
  включать его в диагностику или журнал.

Критерий готовности: после настройки интеграции интерфейс показывает понятное
имя контроллера и подтверждённые сведения о модели, плате, прошивке и серийном
номере; последующие сбои обновления не приводят к ошибке загрузки, а последние
полученные сведения сохраняются.

## v0.3.1 — диагностика и общий слой данных

Статус: **реализовано**.

- добавить состояние связи контроллера с облаком ZONT и всех активных каналов
  GSM, Wi‑Fi и Ethernet;
- добавить измерение напряжения питания контроллера;
- добавить штатную кнопку перезапуска устройства;
- выполнять первый опрос в фоне, периодический контрольный опрос с интервалом
  по умолчанию одна минута и немедленное обновление после восстановления
  соединения;
- ввести единый типизированный `DataUpdateCoordinator` и неизменяемый снимок
  данных контроллера;
- подготовить для координатора внутреннюю подписку на незапрошенные
  WebSocket-сообщения и событие `zont_local_event`;
- изолировать неподдерживаемые системные команды, чтобы один источник данных не
  нарушал обновление остальных сущностей.

Критерий готовности: диагностические сущности не задерживают загрузку Home
Assistant, предсказуемо отражают доступность и автоматически восстанавливаются;
единый координатор готов принять реестр объектов и их push-состояния в серии
`v0.4.x`.

## v0.4.x — обнаружение объектов и сущности только для чтения

Статус: **реализовано**. Серия завершена версией `v0.4.4`.

Общая основа серии уже реализована:

- список объектов каждого поддерживаемого типа получается через `req_ids`, а
  начальные состояния — через `req_state`;
- типизированные модели объектов входят в общий неизменяемый снимок
  координатора без отдельного планировщика обновлений;
- входящие сообщения клиента объединяются с последним состоянием как
  push-обновления, а периодический опрос используется для контрольной сверки;
- дочерние устройства и сущности получают стабильные идентификаторы и могут
  добавляться без перезагрузки Home Assistant;
- ошибки одного типа объектов не прерывают обновление остальных типов.

Первый шаг, `v0.4.0`:

Статус: **реализовано**.

- обнаруживать адаптеры цифровой шины `type=6` и представлять каждый адаптер
  отдельным дочерним устройством контроллера;
- получать температуры теплоносителя, ГВС и обратки, модуляцию, давление,
  состояние и код ошибки;
- создавать только подтверждённые фактическим ответом поля и обновлять их по
  push-сообщениям между контрольными опросами.

Второй шаг, `v0.4.1`:

Статус: **реализовано**.

- обнаруживать цифровые датчики температуры `type=1` и создавать для них
  отдельные дочерние устройства;
- отображать температуру в градусах Цельсия, а поле `a` использовать для
  доступности;
- не выводить `trig` до подтверждения его семантики для этого типа объекта.

Третий шаг, `v0.4.2`:

Статус: **реализовано**.

- обнаруживать аналоговые входы `type=0` всех документированных подтипов
  `stype=0…21` и создавать для них отдельные дочерние устройства;
- отображать числовое значение `v` с единицей `u` и бинарное состояние
  сработки `trig`, назначая подходящие классы устройств Home Assistant;
- учитывать доступность `a`, сохранять последние данные при отключении входа и
  корректно объединять частичные push-сообщения;
- оставлять сырое числовое значение дискретных и охранных входов отключённой по
  умолчанию диагностической сущностью.

Четвёртый шаг, `v0.4.3`:

Статус: **реализовано**.

- обнаруживать NTC-термодатчики `type=27` и создавать для них отдельные
  дочерние устройства;
- отображать температуру в градусах Цельсия и использовать поле `a` для
  доступности;
- объединить общую обработку температурных датчиков, сохранив существующие
  идентификаторы сущностей `type=1`;
- не выводить `trig` до подтверждения его семантики для температурных датчиков.

Пятый шаг, `v0.4.4`:

Статус: **реализовано**.

- обнаруживать радиодатчики `type=8`, сохраняя все корректно описанные
  протоколом подтипы во внутренней модели;
- создавать дочерние устройства и сущности только для радиотермометров
  `stype=5` и `stype=15`, датчика протечки `stype=10`, датчика движения
  `stype=11`, а также датчика температуры и влажности `stype=18`;
- отображать подтверждённые поля температуры, влажности, напряжения батареи,
  уровня радиосигнала и сработки с нативными классами устройств Home Assistant;
- создавать фиксированный для подтипа набор сущностей и корректно объединять
  частичные push-сообщения, не теряя последние значения при `a=0`;
- не создавать публичные сущности для радиореле, розеток, панелей, брелоков и
  остальных подтипов до подтверждения их состояний и безопасных сценариев.

Критерий готовности выполнен: поддерживаемые объекты автоматически появляются
после настройки интеграции, получают актуальные состояния и корректно
становятся недоступными при потере связи.

## v0.5.x — отопление

Статус: **реализовано**. Серия завершена версией `v0.5.5`.

Общая основа серии:

- обнаруживать контуры отопления `type=16`, создавая публичные сущности для
  подтверждённых подтипов ГВС `stype=1` и потребительского контура `stype=3`;
- использовать типизированную модель полей `c`, `s`, `m`, `m_id` и `f`,
  корректно объединяя полные ответы и частичные push-сообщения;
- отправлять команды через отдельный протокольный слой, проверять `cmdres` и
  повторно читать фактическое состояние без оптимистичного обновления.

Первый шаг, `v0.5.0`:

Статус: **реализовано**.

- создавать отдельное дочернее устройство и `water_heater` для каждого
  контура ГВС `stype=1`;
- отображать текущую и целевую температуру и доступность;
- изменять только целевую температуру в диапазоне `5–75 °C` с шагом
  интерфейса `1 °C`;
- не подменять `m=heat` неподтверждённым режимом `water_heater` и не добавлять
  управление включением, режимами или пресетами.

Второй шаг, `v0.5.1`:

Статус: **реализовано**.

- создавать отдельное дочернее устройство и `climate` для каждого
  потребительского контура `stype=3`;
- отображать текущую и целевую температуру, а режим `m` использовать только
  как наблюдаемое состояние без неподтверждённого управления включением;
- изменять уставку с шагом интерфейса `1 °C` и получать фактическое состояние
  без оптимистичного обновления;
- вычислять диапазон из `#Z` и `#Y` с учётом регулирования по воздуху,
  воздуху с ПИД, теплоносителю и ПЗА;
- обновлять конфигурацию после подключения и `CFG_RELOAD_REQ`, кэшировать
  пороги датчиков и изолировать ошибки отдельных объектов;
- оставлять контур доступным только для чтения, если безопасный диапазон не
  подтверждён или прямое управление не поддерживается конфигурацией.

Третий шаг, `v0.5.2`:

Статус: **реализовано**.

- отображать способ регулирования и наличие ПЗА как диагностические сущности;
- публиковать блокировку контура, неисправность датчика и летний режим из
  подтверждённых битов внутреннего состояния `#Y`;
- публиковать общую неисправность контуров ГВС и потребительских контуров из
  поля `f` WebSocket-состояния;
- сохранять раздельную доступность данных объекта, конфигурации `#Z` и
  внутреннего состояния `#Y`.

Четвёртый шаг, `v0.5.3`:

Статус: **реализовано**.

- обнаруживать режимы отопления `type=20` и читать их имена, связанные контуры
  и уставки из `#Z<mode_id>`;
- определять применимые к каждому контуру режимы по его внутреннему состоянию
  `#Y<circuit_id>`;
- при добавлении интеграции обязательно выбирать режим, который содержит
  нулевые уставки для всех контуров ГВС и потребительских контуров;
- предоставлять для потребительского контура стандартные `heat` и `off`,
  применяя режим отключения только к выбранному контуру и проверяя фактическое
  состояние после `cmdres`;
- при включении восстанавливать запомненный в текущей сессии режим или уставку,
  а после перезапуска использовать минимальную допустимую температуру;
- не определять назначение режима по имени, порядковому номеру или фиксированному
  ID: режимы можно переименовывать и настраивать независимо;
- обновлять конфигурацию режимов после подключения и `CFG_RELOAD_REQ`.

Пятый шаг, `v0.5.4`:

Статус: **реализовано**.

- сопоставлять наблюдаемые состояния ГВС `m=heat` и `m=off` со стандартными
  состояниями `on` и `off` сущности `water_heater`;
- предоставлять стандартные действия включения и отключения только при
  подтверждённой безопасной привязке режима отключения;
- применять режим отключения только к выбранному контуру ГВС и подтверждать
  его `m_id` и состояние адресным чтением;
- при включении восстанавливать последнюю активную уставку текущей сессии, а
  после перезапуска использовать настраиваемую температуру `5–75 °C` с
  безопасным значением по умолчанию `60 °C`;
- считать включение успешным только после подтверждения `m=heat` и целевой
  температуры, не обновляя состояние оптимистично.

Шестой шаг, `v0.5.5`:

Статус: **реализовано**.

- настраивать интервал контрольного полного опроса от `10` до `120 секунд`
  при добавлении интеграции и через options flow;
- использовать `60 секунд` по умолчанию для новых, старых и некорректно
  настроенных записей;
- применять новый интервал без перезагрузки config entry и разрыва WebSocket,
  сохраняя немедленную обработку push-сообщений.

## v0.6.x — исполнительные устройства

Статус: **реализовано**. Серия завершена версией `v0.6.2`.

Первый шаг, `v0.6.0`:

Статус: **реализовано**.

- обнаруживать насосы `type=17` и создавать для каждого отдельное дочернее
  устройство;
- показывать фактическую работу насоса из поля `s` основным `binary_sensor` с
  классом `running`;
- обновлять состояние через общий контрольный опрос и push-сообщения;
- не предоставлять управление насосом: контроллер учитывает запрос контура и
  настроенный выбег, поэтому принятие команды не гарантирует физического
  переключения.

Второй шаг, `v0.6.1`:

Статус: **реализовано**.

- обнаруживать смесители `type=15` и создавать для каждого отдельное дочернее
  устройство;
- показывать оперативные состояния **В покое**, **Открывается** и
  **Закрывается** из поля `s`, а концевые положения — из подтверждённых флагов
  ответа `#Y<id>?`;
- читать внутреннее состояние во время общего контрольного опроса через то же
  WebSocket-соединение и изолировать ошибку одного смесителя;
- сбрасывать устаревшее концевое положение при начале движения;
- публиковать диагностические признаки неисправности датчика, неисправности
  выхода и ошибки установки;
- оставить смеситель только для чтения до подтверждения безопасных команд и
  ошибочных сценариев позиционного управления.

Третий шаг, `v0.6.2`:

Статус: **реализовано**.

- обнаруживать реле `type=14` и создавать для каждого отдельное дочернее
  устройство и основную сущность `switch`;
- учитывать оба подтверждённых флага инверсного режима из `#Z<id>?` и не
  фильтровать реле по наличию привязанного аппаратного выхода;
- включать и выключать реле командами `cmd=1` и `cmd=0`, проверять `cmdres` и
  подтверждать логическое состояние адресным `req_state` без оптимистичного
  обновления;
- кэшировать конфигурацию инверсии до переподключения или `CFG_RELOAD_REQ`, а
  внутренний регистр `#Y<id>?` читать при каждом контрольном опросе;
- публиковать диагностический признак неисправности и изолировать ошибки
  внутренних ответов одного реле от остальных объектов.

Позиционное управление смесителем и другие сложные исполнительные сущности
имеют статус **требует подтверждения протокола**. Для каждой такой сущности
сначала должны быть подтверждены все состояния, команды и ошибочные сценарии.

## v0.7 — выбор импортируемых устройств

Статус: **реализовано**.

- выбирать при добавлении интеграции, какие поддерживаемые объекты ZONT нужно
  представить дочерними устройствами и сущностями Home Assistant;
- изменять выбор позже через отдельный раздел options flow, не смешивая его с
  режимом отключения отопления и интервалом опроса;
- включать автоматический импорт новых объектов по умолчанию, сохраняя явно
  исключённые ID в списке исключений;
- удалять исключённые дочерние устройства и сущности из реестров Home Assistant
  и восстанавливать стабильные `unique_id` при повторном импорте;
- применять изменённый выбор точечно без перезагрузки config entry: не разрывать
  WebSocket и не делать остальные сущности временно недоступными;
- продолжать полный протокольный опрос всех поддерживаемых объектов независимо
  от их публикации в Home Assistant;
- сохранять прежний автоматический импорт для существующих записей без новых
  настроек.

## v0.8 — экспорт температурных сенсоров в ZONT

Статус: **реализовано**.

- настраивать экспорт в отдельном разделе настроек интеграции, не смешивая его
  с импортом объектов контроллера;
- выбирать источником только сущность `sensor` с классом устройства
  `temperature`, преобразовывать поддерживаемые единицы Home Assistant в
  градусы Цельсия и округлять значение до `0,1 °C`;
- создавать новый цифровой датчик температуры ZONT `type=1` именованной
  JSON-командой либо явно привязывать существующий объект `type=1`;
- перед сохранением привязки проверять тип цели и выполнять подтверждённую
  первую запись, сохраняя стабильный ID созданного или выбранного объекта;
- отслеживать переименование источника через реестр сущностей Home Assistant;
- передавать новое значение сразу после изменения состояния, повторять
  последнее корректное значение каждые `120 секунд` и синхронизироваться после
  восстановления WebSocket-соединения;
- использовать единственное рабочее соединение интеграции и не открывать
  параллельный WebSocket для настройки экспорта;
- применять добавление, изменение и удаление привязок на работающем менеджере
  экспорта без перезагрузки config entry и замены общего клиента;
- повторно проверять цель через `req_state`; при удалении объекта, смене его
  типа или отклонении команды останавливать конкретную привязку и создавать
  запись о проблеме в Home Assistant без автоматического пересоздания объекта;
- исключать цели экспорта из обычного импорта устройств ZONT, а при удалении
  или изменении привязки оставлять прежний объект в контроллере и в списке
  исключений;
- не использовать `#Y` и `#Z` для экспорта: подтверждённых JSON-команд
  создания, записи и проверки состояния достаточно.

## v0.9 — расширенная локальная диагностика

Статус: **реализовано**.

- получать состояния Wi-Fi, Ethernet и GSM через `#S198?`, `#S205?` и `#S4?`,
  а источник питания — из второго поля `#S6?`;
- публиковать подключение Wi-Fi и Ethernet, регистрацию GSM и источник питания
  штатными диагностическими сущностями Home Assistant;
- добавлять необязательные сущности только после первого корректного ответа и
  считать точный ответ `!` ожидаемым отсутствием поддержки без warning и
  повторного опроса до reload;
- публиковать уровни Wi-Fi и GSM в процентах отдельными диагностическими
  сенсорами, отключёнными по умолчанию;
- не публиковать и не включать в скачиваемую диагностику MAC-адрес, IP-адрес,
  маску, шлюз и название мобильного оператора;
- выполнять запросы в общем последовательном цикле обновления, используя одно
  WebSocket-соединение и изолируя сбой одного источника от остальных.

## v0.10 — расширенное представление отопления

Статус: **реализовано**. Серия завершена версией `v0.10.3`.

### v0.10.0 — режимы потребительских контуров

Статус: **реализовано**.

- отображать применимые именованные режимы потребительского контура как
  `preset_modes`, сохраняя пользовательские названия из ZONT;
- строить список режимов по пересечению применимых ID из `#Y` и конфигураций
  `#Z type=20`, включая режимы с нулевой уставкой для конкретного контура;
- определять текущий `preset_mode` по `m_id`, а `m_id=0` считать ручным
  управлением без активного preset;
- при совпадении пользовательских названий однозначно дополнять только
  конфликтующие варианты ID режима;
- переключать только выбранный контур командой с его ID и ID режима, проверяя
  `cmdres`, фактический `m_id` и итоговое состояние без оптимистичного
  обновления;
- сохранить существующую привязку режима отключения для стандартных
  `turn_on` и `turn_off`, не смешивая её с выбором preset.

### v0.10.1 — режимы ГВС

Статус: **реализовано**.

- представить применимые режимы ГВС штатными operation modes сущности
  `water_heater`;
- показывать пользовательское название текущего режима по `m_id`, отдельно
  различая ручную уставку и выключенное состояние;
- применять выбранный режим только к контуру ГВС и подтверждать итоговое
  состояние контроллера;
- сохранить стандартные действия включения, отключения и установки
  температуры.

### v0.10.2 — оперативные состояния контуров

Статус: **реализовано**.

- показывать фактический запрос тепла потребительского контура штатным
  `hvac_action`, различая `heating`, `idle` и `off` независимо от режима `m`;
- публиковать для ГВС основной binary sensor нагрева с классом `running`;
- публиковать для каждого потребительского контура расчётную или требуемую
  температуру теплоносителя из поля `0` ответа `#Y` независимо от ПЗА и способа
  регулирования;
- считать летний режим оперативным состоянием, а не диагностической сущностью;
- использовать существующее контрольное чтение `#Y`, не создавая отдельного
  соединения или цикла опроса и не связывая доступность основных сущностей с
  расширенными данными.

### v0.10.3 — единая диагностика неисправностей

Статус: **реализовано**.

- сохранить общую неисправность контуров отопления и реле;
- добавить адаптеру цифровой шины общий binary sensor по состоянию ошибки или
  ненулевому коду `err`;
- добавить смесителю общий binary sensor как объединение трёх подтверждённых
  аварийных флагов, сохранив подробные диагностические сущности;
- не считать неисправностью временную недоступность, остановку насоса или
  обычную сработку входа;
- использовать существующие JSON- и `#Y`-данные без дополнительных запросов.

## v0.11 — ZONT Local и актуализация режимов

Статус: **частично реализовано**. Переименование завершено в `v0.11.0`, экспорт
датчиков открытия — в `v0.11.1`, универсальный бинарный экспорт — в `v0.11.2`;
актуализация конфигурации режимов остаётся следующим шагом серии.

### v0.11.0 — переименование в ZONT Local

Статус: **реализовано**.

- переименовать пользовательскую интеграцию в ZONT Local;
- изменить технический домен и каталог с `zont_ws` на `zont_local` без
  автоматической миграции config entry и совместимых псевдонимов;
- переименовать действия в `zont_local.send_command` и
  `zont_local.send_bulk`, а событие — в `zont_local_event`;
- документировать обязательное удаление прежней записи до замены файлов и
  повторное добавление контроллера;
- сохранить термин WebSocket только для фактического локального транспорта.

### v0.11.1 — экспорт датчиков открытия

Статус: **реализовано**.

- расширить единый раздел экспорта источниками `binary_sensor` с классами
  `door`, `window`, `opening` и `garage_door`;
- создавать виртуальные дискретные входы НЗ `type=0`, `stype=20` либо
  привязывать существующие объекты того же типа;
- передавать закрытие командой `0 0 180`, открытие — `0 20 180`, а недоступное
  состояние не продлевать;
- использовать общий менеджер, heartbeat `120 секунд` и рабочий WebSocket
  температурных привязок;
- исключать цели из обычного импорта и применять изменения без reload config
  entry;
- не использовать `stype=3`: на виртуальном входе он меняет напряжение, но не
  пересчитывает `trig`.

### v0.11.2 — универсальный бинарный экспорт

Статус: **реализовано**.

- разрешить экспорт любого внешнего `binary_sensor`, включая сущности без
  `device_class`;
- создавать виртуальные дискретные входы НР (`stype=19`) и НЗ (`stype=20`) с
  обязательным выбором типа;
- передавать `off` значением `0`, а `on` — значением `20`, оставляя типу входа
  ZONT определение состояния сработки;
- показывать оба подтверждённых подтипа при привязке и сохранять точный подтип
  цели для последующей проверки;
- не добавлять миграцию тестовых привязок `kind=opening`.

### v0.11.3 — актуализация конфигурации режимов

- немедленно перечитывать конфигурацию при изменении состава ID режимов;
- обновлять кэш режимов после переподключения и контрольным редким опросом,
  ориентировочно раз в `15 минут`, чтобы подхватывать переименование и
  изменение уставок без лишней нагрузки на контроллер;
- использовать единственное рабочее WebSocket-соединение интеграции.

Индивидуальное применение именованного режима к контуру и связь текущего
режима с `m_id` подтверждены. Самостоятельная оперативная команда выключения
контура без применения режима пока не найдена ни в документации, ни в
исследованной версии официального веб-клиента. Конфигурационное исключение
контура из работы не рассматривается как замена обычному `turn_off`.

Глобальный и временный режимы, таймер, сброс временного режима и изменение
конфигурации режимов остаются исследовательскими задачами. Управление ими
добавляется только после подтверждения команд, единиц длительности и поведения
при конфликте с ручными уставками отдельных контуров.

## v0.12 — охранные зоны

Статус: **запланировано**, **требует подтверждения протокола**.

- получить и обезличить реальные полные ответы и push-сообщения охранных зон
  `type=2`;
- зафиксировать в документации поля состояния, доступности, тревоги,
  неисправности и постановки на охрану;
- добавить типизированную модель и отдельные дочерние устройства для зон;
- создавать `binary_sensor` только для подтверждённых состояний;
- рассматривать `alarm_control_panel` и команды постановки или снятия с охраны
  только после проверки всех состояний, команд и ошибочных сценариев на
  реальном контроллере.

## v0.13 — радиомодуль

Статус: **запланировано**. Управляющие возможности **требуют подтверждения
протокола**.

- обнаруживать радиомодули `type=7` и создавать для них отдельные дочерние
  устройства;
- публиковать подтверждённые диагностические состояния связи с модулем,
  режима добавления, ошибок связи и времени последнего подключения;
- использовать список связанных датчиков только для диагностики и проверки
  обнаружения, не дублируя связи реестра устройств Home Assistant;
- не добавлять очистку радиомодуля и режимы регистрации устройств в обычные
  сущности интеграции: эти операции изменяют конфигурацию и требуют отдельного
  защищённого пользовательского сценария.

## v0.14 — дополнительные радиоустройства

Статус: **запланировано**, **требует подтверждения протокола**.

- получить реальные состояния и конфигурацию пока не поддерживаемых подтипов
  `type=8`, в первую очередь:
  - радиорозетки;
  - составного термодатчика с тремя каналами и концевым контактом;
  - счётчиков воды, газа и электроэнергии;
  - радиопанели или радиотермостата;
- исследовать радиодатчики 433 МГц `type=31` только для моделей, где они
  поддерживаются аппаратно;
- создавать `switch`, `sensor`, `binary_sensor` или `climate` только после
  подтверждения полей состояния, доступности, единиц измерения и безопасных
  команд конкретного подтипа;
- не включать добавление, привязку или удаление радиоустройств в штатный опрос
  и управление интеграции.

## v0.15 — пользовательские элементы

Статус: **запланировано**, **требует реального объекта и подтверждения
протокола**.

- обнаруживать пользовательские элементы `type=10` и читать их конфигурацию и
  фактическое состояние;
- представлять подтверждённые типы элементов штатными сущностями Home
  Assistant:
  - `switch` для фиксируемых двухпозиционных элементов;
  - `button` для простых кнопок без сохраняемого состояния;
  - `number` для аналоговых регуляторов с подтверждёнными границами и шагом;
  - `select` только если реальный элемент имеет конечный набор подтверждённых
    значений;
- использовать команду изменения состояния из официального клиента, проверять
  результат и повторно читать фактическое состояние без оптимистичного
  обновления;
- не определять тип сущности только по названию элемента и не создавать
  управляемую сущность при неоднозначной конфигурации.

На исследуемом контроллере запрос `req_ids:10` не возвращает объектов, поэтому
этап отложен до появления реальных конфигурации, состояния и push-сообщений
хотя бы одного пользовательского элемента.

## v0.16 — надёжность и готовность к HACS

Статус: **запланировано**.

- добавить CI для тестов, Ruff, проверки конфигурации Home Assistant, Hassfest
  и HACS;
- поддерживать покрытие автоматическими тестами не ниже 95%;
- расширить безопасную диагностику и использовать Repairs для ошибок, которые
  требуют вмешательства пользователя;
- проверить восстановление после обрывов, перезапуска контроллера и смены
  учётных данных;
- документировать поддерживаемые устройства, ограничения, удаление интеграции
  и устранение неполадок;
- добавить `hacs.json`, обязательные поля manifest, бренд и воспроизводимый
  процесс релизов.

Критерий готовности: проверки HACS и Home Assistant проходят без исключений,
диагностика не раскрывает секреты, а типовые сбои не требуют перезапуска Home
Assistant и не засоряют журнал повторяющимися сообщениями.

## v1.0 — стабильный выпуск

Статус: **запланировано**.

- опубликовать матрицу поддерживаемых контроллеров и прошивок;
- сохранить совместимость действий `zont_local.send_command` и
  `zont_local.send_bulk`, конфигурации и идентификаторов сущностей;
- предусмотреть явные миграции для несовместимых изменений;
- подготовить полный пользовательский README и стабильный процесс выпусков
  через HACS;
- считать стабильными только функции, подтверждённые документацией, реальным
  устройством и автоматическими тестами.

## Отложенные возможности

В roadmap намеренно не назначены версии следующим возможностям:

- несколько контроллеров в одной установке Home Assistant;
- автоматическое обнаружение контроллера в локальной сети;
- обновление прошивки из Home Assistant;
- дополнительные терминальные команды `scmd`;
- служебные объекты расписаний `type=21` и `type=22`, отсутствующие в
  официальной таблице WebSocket API;
- сущности для неподтверждённых типов и подтипов объектов.

Они будут запланированы после появления подтверждённого протокола или реального
пользовательского сценария. Команды, основанные только на предположениях, не
будут добавляться даже как экспериментальная настройка.

Файловый менеджер контроллера, форматирование файловой системы, произвольная
запись `config.txt`, добавление и удаление радиоустройств, звонки и SMS не
планируются как публичные сущности или универсальные действия интеграции. Эти
операции либо разрушающие, либо способны нарушить работу отопления, изменить
конфигурацию, раскрыть секретные данные или создать внешние расходы.

Котловые контуры `stype=0` и контуры охлаждения `stype=2` сохраняются только во
внутренней модели протокола. Создание для них публичных сущностей и управление
ими в roadmap не планируются.

## Принципы развития

- Источники протокола изучаются в порядке, зафиксированном в `AGENTS.md`.
- Предпочтение отдаётся нативным сущностям и жизненному циклу Home Assistant.
- Универсальные действия сохраняются как низкоуровневый совместимый интерфейс.
- Архитектурные решения сверяются с
  [Integration Quality Scale](https://developers.home-assistant.io/docs/core/integration-quality-scale/).
- Подготовка публикации выполняется по
  [требованиям HACS](https://www.hacs.xyz/docs/publish/integration/).
